provider_installation {
  dev_overrides {
    "geico-private/pkg" = "/Volumes/Development/terraform-package"
  }
  
  # For all other providers, install them directly from their origin provider
  # registries as normal.
  direct {}
}
